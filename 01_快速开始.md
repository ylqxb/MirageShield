# 快速开始

## 环境准备

### 系统要求
- **操作系统**：Windows 10+（推荐 Windows 11，仅在此环境测试通过）或 Linux (未测试)
- **Python**：3.8~3.11（推荐 3.9 或 3.10，3.12+ 可能存在 asyncio 兼容性问题）。如遇问题，请使用 Python 3.10 或 3.11。
- **硬件**：推荐 4 核 CPU，8GB 内存，50GB 存储空间（最低配置 2 核/4GB/20GB 可运行学习测试，但可能影响性能）。
- **网络**：稳定的网络连接，支持 IPv4/IPv6
- **Docker**：用于部署蜜罐（可选但推荐，不使用 Docker 会导致蜜罐功能受限）

### 软件安装

**Windows 系统**：
1. 安装 Python 3.8+：从 [Python 官网](https://www.python.org/downloads/) 下载并安装
2. 安装 Git：从 [Git 官网](https://git-scm.com/downloads) 下载并安装
3. 安装 Docker Desktop：从 [Docker 官网](https://www.docker.com/products/docker-desktop) 下载并安装
4. 配置环境变量：确保 Python 和 Git 都已添加到系统 PATH

**Linux 系统**：
```bash
# 安装 Python 3.8+
sudo apt update
sudo apt install python3 python3-pip python3-venv

# 安装 Git
sudo apt install git

# 安装 Docker
sudo apt install docker.io
sudo systemctl start docker
sudo systemctl enable docker
sudo usermod -aG docker $USER
```

## 一键部署

### 方法一：使用一键部署脚本（推荐）

**Windows 系统**：
1. 确保已安装 Python 3.8+ 和 Docker Desktop
2. 下载项目代码后，在项目根目录双击运行 `deploy_windows.bat`
3. 脚本会自动检查环境、构建镜像并启动服务
4. 部署完成后，根据提示访问服务地址

**Linux 系统**：
1. 确保已安装 Python 3.8+、Docker 和 docker-compose
2. 下载项目代码后，在项目根目录执行：
   ```bash
   chmod +x deploy_linux.sh
   ./deploy_linux.sh
   ```
3. 脚本会自动检查环境、构建镜像并启动服务
4. 部署完成后，根据提示访问服务地址

### 方法二：手动部署

1. **克隆代码库**
```bash
git clone https://github.com/ylqxb/MirageShield.git
cd MirageShield
```

2. **创建并激活虚拟环境**
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux
python3 -m venv venv
source venv/bin/activate
```

3. **安装依赖**
```bash
pip install -r requirements.txt
```

4. **配置环境变量**
创建 `.env` 文件：
```dotenv
# 管理员密码
ADMIN_PASSWORD=your_secure_password

# 加密密钥
ENCRYPTION_KEY=your_encryption_key

# 水印密钥
WATERMARK_KEY=your_watermark_key

# 可选：外部 API 密钥（预留接口，当前版本可能尚未完全实现）
# TOR_CONTROL_PASSWORD=your_tor_password
# IP_REPUTATION_API_KEY=your_ip_reputation_api_key
# OPENAI_API_KEY=your_openai_api_key
```

> **注意**：上述外部 API 功能为预留接口，当前版本可能尚未完全实现，配置后可能不会立即生效。具体支持情况请关注项目更新日志。

5. **启动系统**
```bash
python start_server.py
```

## 验证部署

1. **访问 Web 界面**：http://localhost:5000
2. **使用设置的管理员密码登录**
3. **检查系统状态**：
   - 查看智能体状态是否为 "active"
   - 检查防护级别设置是否正常
   - 测试硬件管理功能

## 核心依赖清单

| 依赖包 | 版本要求 | 用途 |
|-------|---------|------|
| Flask | >=2.0.0 | Web 框架 |
| psutil | >=5.8.0 | 系统监控 |
| cryptography | >=36.0.0 | 加密功能 |
| docker | >=5.0.0 | 蜜罐部署 |
| asyncio | 内置 | 异步编程 |
| loguru | >=0.5.0 | 日志管理 |

## 常见问题

### 端口被占用
- **问题**：启动时提示 5000 端口被占用
- **解决**：检查并关闭占用 5000 端口的进程，或修改 `start_server.py` 中的端口配置

### 依赖安装失败
- **问题**：pip 安装依赖时失败
- **解决**：确保网络连接正常，尝试使用国内镜像源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 智能体启动失败
- **问题**：智能体状态显示为 "inactive"
- **解决**：检查配置文件是否正确，确保环境变量已设置

## 下一步

- 查看 [05_用户手册.md](./05_用户手册.md) 了解系统使用方法
- 查看 [02_部署指南.md](./02_部署指南.md) 了解详细部署流程
- 查看 [07_常见问题FAQ.md](./07_常见问题FAQ.md) 解决常见问题

---

**最后更新时间**：2026-03-23
**适配版本**：MirageShield v1.0